version: 2.1

anchors:
  hello: &hello
    - run: echo "Hello"

orbs:
  android: circleci/android@0.2.1

jobs:
  build:
    executor: android/android

    steps:
      - checkout
      - run: ./gradlew build
      - <<: *hello

workflows:
  version: 2.1
  build:
    jobs:
      - build:
          filters:
            branches:
              only: master
